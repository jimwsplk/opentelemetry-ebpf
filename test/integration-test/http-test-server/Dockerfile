# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

FROM nginx-test-server

ARG HTTP_RESPONSE_CODE
ENV HTTP_RESPONSE_CODE=${HTTP_RESPONSE_CODE}

ARG INCOMING_PORT
ENV INCOMING_PORT=${INCOMING_PORT}
ENV HEALTH_CHECK_PORT=${INCOMING_PORT}
EXPOSE ${INCOMING_PORT}
